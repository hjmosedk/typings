{
  "name": "ckh-typings",
  "version": "0.1.0",
  "description": "this is the typescript typings for my project. they have been added to a common repository to ensure reusability and consistency",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky",
    "release": "standard-version --sign",
    "release:minor": "standard-version --release-as minor --sign",
    "release:patch": "standard-version --release-as patch --sign",
    "release:major": "standard-version --release-as major --sign"
  },
  "keywords": [
    "typings",
    "ts",
    "js"
  ],
  "author": "Christian Kubel Højmose",
  "license": "AGPL-3.0-or-later",
  "devDependencies": {
    "@types/node": "^20.12.2",
    "typescript": "^5.4.3"
  },
  "dependencies": {
    "@commitlint/cli": "^19.2.1",
    "@commitlint/config-conventional": "^19.1.0",
    "husky": "^9.0.11",
    "standard-version": "^9.5.0"
  }
}
